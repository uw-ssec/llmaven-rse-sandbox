{
  "name": "LLMaven RSE Sandbox",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "GitHub.copilot",
        "GitHub.copilot-chat",
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "workbench.startupEditor": "readme",
        "files.watcherExclude": {
          "**/.git/objects/**": true,
          "**/.git/subtree-cache/**": true,
          "**/node_modules/*/**": true
        }
      }
    }
  },
  "secrets": {
    "LITELLM_BASE_URL": {
      "description": "Base URL for the LLMaven / LiteLLM gateway"
    },
    "LITELLM_API_KEY": {
      "description": "API key for the LLMaven / LiteLLM gateway"
    }
  },
  "mounts": [
    "source=${localWorkspaceFolderBasename}-pixi,target=${containerWorkspaceFolder}/.pixi,type=volume"
  ],
  "remoteEnv": {
    "LITELLM_BASE_URL": "${localEnv:LITELLM_BASE_URL}",
    "LITELLM_API_KEY": "${localEnv:LITELLM_API_KEY}",
    // NOTE: OAI_API_KEY is aliased from LITELLM_API_KEY for Copilot compatibility.
    // This value is available to all container processes including extensions.
    "OAI_API_KEY": "${localEnv:LITELLM_API_KEY}",
    "RSE_EXTENSION_REF": "0a6f01e8213985d075223a99be67824ac0e1ced8"
  },
  "onCreateCommand": "bash .devcontainer/on-create.sh",
  "postCreateCommand": "bash .devcontainer/post-create.sh",
  "postStartCommand": "bash .devcontainer/post-start.sh",
  "remoteUser": "vscode"
}
